const renderMain = require('./main');
const renderLogin = require('./login');
const renderRegister = require('./register');
const renderAddHouse = require('./addHouse');
const renderUpdate = require('./update');


module.exports = {
    renderMain,
    renderLogin,
    renderRegister,
    renderAddHouse,
    renderUpdate
};

